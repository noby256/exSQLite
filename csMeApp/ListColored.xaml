<UserControl x:Class="csMeApp.ListViewColored"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <ListView SelectionChanged="ListBox_SelectionChanged" MouseRightButtonUp="ListBox_MouseRightButtonUp">
            <ListView.View>
                <GridView>
                    <!--<GridViewColumn Header="Name" Width="120" DisplayMemberBinding="{Binding Name}" />
                    <GridViewColumn Header="Age" Width="50" DisplayMemberBinding="{Binding Age}" />
                    <GridViewColumn Header="Mail" Width="150" DisplayMemberBinding="{Binding Mail}" />-->
                    <!--<ListViewItem Content="ListBoxItem" BorderThickness="3" Background="#3368CF1F" BorderBrush="#FF46FF00"/>
            <ListViewItem Content="ListBoxItem"/>
            <ListViewItem IsSelected="True">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Width="200*">Red</TextBlock>
                    <TextBlock Width="50*" >Red</TextBlock>
                </StackPanel>
            </ListViewItem>-->
                 </GridView>
            </ListView.View>
        </ListView>

    </Grid>
</UserControl>
